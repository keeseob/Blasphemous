#pragma once

enum class eSceneType
{
	title,
	play,
	ending,
	end,
};

enum class eLayerType
{
	background,
	monster,
	player,
	effect,
	UI,
	end,
};

enum class eComponentType
{
	transformation,
	collision,
	spriteRender,
	animation,
	audio,
	end,
};